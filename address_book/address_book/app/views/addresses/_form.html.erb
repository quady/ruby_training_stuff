<%= form_for(@address) do |f| %>
  <% if @address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address.errors.count, "error") %> prohibited this address from being saved:</h2>

      <ul>
      <% @address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <p>
      <%= f.label :street %>
      <%= f.text_field :street %>
    </p>

    <p>
      <%= f.label :zip %>
      <%= f.text_field :zip %>
    </p>

    <p>
      <%= f.label :city %>
      <%= f.text_field :city %>
    </p>

    <p>
      <%= f.label :state %>
      <%= f.text_field :state %>
    </p>

    <p>
      <%= f.label :user_id %>
      <%= f.select(:user_id, options_from_collection_for_select(User.all, :id, :full_name, @address.user_id)) %>
    </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
